CREATE TABLE animal (
                        id BIGINT AUTO_INCREMENT PRIMARY KEY,
                        name VARCHAR(255) NOT NULL,
                        type VARCHAR(100) NOT NULL,
                        age INT NOT NULL,
                        description TEXT
);
